# Remittance Tracker

This project is an interactive dashboard for tracking loans and payments.
- Loans are recorded in `loans.csv`.
- All payments and balances are logged in `ledger.csv`.
- Dashboard interface is provided by `dashboard.py` using Streamlit.

## Usage

```bash
pip install streamlit
streamlit run dashboard.py
